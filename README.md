A simple (and incomplete) program that creates a frequency table from a dataset.

Why this exists?

This started because I didn’t really feel like manually counting numbers in a dataset we got from one of our classes. It was slow, boring, and honestly pretty easy to mess up.

After I finished the counting algorithm, I thought, why stop there?

pretty incomplete. might break with improper input.
